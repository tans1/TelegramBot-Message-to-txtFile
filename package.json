{
  "name": "telegrambot-message-to-txtfile",
  "version": "1.0.0",
  "description": "a telegram bot that receives message and sends it back writing on .txt file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "nodemon": "^3.0.1",
    "request": "^2.88.2",
    "telegraf": "^4.12.2"
  }
}
